# Quy tắc component

**Bảng mô tả thay đổi**

A – Tạo mới, M – Sửa đổi, D – Xóa bỏ

|  |  |  |  |  |  |  |  |  |
| :-- | :-- | :-- | :-- | :-- | :-- | :-- | :-- | :-- |
| Ngày tạo | Ngày thay đổi | Vị trí thay đổi | Người thực hiện | A*, M, D | Nguồn gốc | Link Jira | Mô tả thay đổi | Phiên bản Confluence |
| 11 Mar 2026 |  |  | Haiht | A |  |  | Tạo mới tài liệu | v.15 |

### **1\. Thông tin chung**

|  |  |
| :-- | :-- |
| Mục đích | Tiêu chuẩn để FE dùng chung cho các màn hình trên toàn bộ hệ thống Core CIC |
| Trường hợp ngoại lệ | Nghiệp vụ nào có yêu cầu đặc biệt, không sử dụng quy tắc chung sẽ được mô tả riêng trong SRS của nghiệp vụ đó |

### **2\. Quy tắc chung**

#### **2.1 Quy chuẩn chung cho toàn bộ hệ thống**

| STT | Loại | Quy tắc chung |
| :-- | :-- | :-- |
| 1 | Ngôn ngữ | Toàn bộ giao diện sử dụng tiếng Việt chuẩn hành chính.Không sử dụng từ viết tắt không định nghĩa. Trong trường hợp viết tắt thì có thể dùng tooltip bổ sung cho người dùng (định nghĩa tại từng nghiệp vụ cụ thể). |
| 2 | Font chữ | Sử dụng Unicode chuẩn TCVN 6909:2001. Font đề xuất: Chờ chốt design. |
| 3 | Định dạng ngày | Hiển thị theo DD/MM/YYYY. Nếu cần hiển thị rõ hơn đến hh:mm:ss thì mô tả rõ tại từng nghiệp vụ, giờ mặc định lấy theo múi giờ GMT +7. Căn giữa khi hiển thị. |
| 4 | Định dạng số | Dùng “.” phân cách hàng nghìn; và dùng "," để phân cách số thập phân.Mặc định nếu không phải số nguyên thì hiện 2 chữ số sau dấu thập phân theo quy tắc làm tròn, nghiệp vụ nào cần nhiều ký tự hơn thì có mô tả cụ thể tại nghiệp vụ đó.Khi hover qua giá trị của số thập phân thì hiển thị tooltip đầy đủ giá trị các chữ số sau dấu thập phân nhưng không quá 5 chữ số phần thập phân.Hệ thống sẽ hỗ trợ nhập, lưu trữ và hiển thị dữ liệu tiền tệ VND với ít nhất 20 chữ số nguyên. |
| 5 | Định dạng Số tiền | Hiển thị khác nhau theo ngôn ngữ của giao diện: - Giao diện tiếng Việt: Dùng “.” phân cách hàng nghìn => Ví dụ: 1.000 Triệu VNĐ - Giao diện tiếng Anh: Dùng "," ngăn cách nghìn và dùng "." ngăn cách số thập phân => Ví dụ: 1,000.25 USD |
| 6 | Căn lề dữ liệu | Text căn trái.Numeric căn phải. (Trong đó bao gồm cả trường Số tiền)Date căn giữa. |
| 7 | Phím tắt | Tất cả màn hình thao tác cần hỗ trợ được bằng chuột và bằng bàn phím.Xem chi tiết Danh sách phím nóng cần cung cấp cho hệ thốngNhóm chức năngPhímChức năngMức độ áp dụngĐiều hướngTabDi chuyển sang trường tiếp theoShift + TabDi chuyển về trường trướcEnterXác nhận / chuyển trường tiếp theoTùy cấu hình theo formF2Vào chế độ chỉnh sửa ô/trườngÁp dụng cho grid/lưới dữ liệuCRUDF3 / Ctrl + NTạo mới bản ghiF2Chỉnh sửa bản ghi đang chọnF5Làm mới / Refresh trangBảng dữ liệu/Dữ liệuCtrl + CCopyCtrl + XCutCtrl + VPasteCtrl + AChọn tất cảXuất excel & In báo cáoCtrl + PIn (Print)Ctrl + Shift + EXuất ExcelGiao diện/Hệ thốngCtrl + TabChuyển giữa các tab mởCtrl + Shift + TabChuyển tab ngượcNhập liệu đặc thùCtrl + KChèn liên kết / Lookup |
| Điều hướng |  |  |
|  | Tab | Di chuyển sang trường tiếp theo |
|  | Shift + Tab | Di chuyển về trường trước |
|  | Enter | Xác nhận / chuyển trường tiếp theo |
|  | F2 | Vào chế độ chỉnh sửa ô/trường |
| CRUD |  |  |
|  | F3 / Ctrl + N | Tạo mới bản ghi |
|  | F2 | Chỉnh sửa bản ghi đang chọn |
|  | F5 | Làm mới / Refresh trang |
| Bảng dữ liệu/Dữ liệu |  |  |
|  | Ctrl + C | Copy |
|  | Ctrl + X | Cut |
|  | Ctrl + V | Paste |
|  | Ctrl + A | Chọn tất cả |
| Xuất excel & In báo cáo |  |  |
|  | Ctrl + P | In (Print) |
|  | Ctrl + Shift + E | Xuất Excel |
| Giao diện/Hệ thống |  |  |
|  | Ctrl + Tab | Chuyển giữa các tab mở |
|  | Ctrl + Shift + Tab | Chuyển tab ngược |
| Nhập liệu đặc thù |  |  |
|  | Ctrl + K | Chèn liên kết / Lookup |
| 8 | Màu sắc | Màu lỗi: Đỏ, Thành công: Xanh, Cảnh báo: Cam/đỏ => Cụ thể hơn chờ chốt design |
| 9 | Tính nhất quán | Các màn hình lọc báo cáo phải đồng bộ layout, nút chức năng.Các màn hình nhập và cập nhật dữ liệu về cơ bản phải thống nhất về các thao tác trên bàn phím cũng như màu sắc, font chữ...Không tồn tại 2 cách hiển thị cho cùng 1 loại dữ liệu.Placeholder nếu có sẽ trùng tên trường điều kiện lọc.Hỗ trợ điều hướng bằng bàn phím (mũi tên, Enter, Esc để đóng). |
| 10 | Responsive | Đảm bảo responsive trên các thiết bị máy tính, tablet. Riêng Web Portal cần đảm bảo thêm trên Mobile. |
| 11 | Khu vực tìm kiếm - MH Tra cứu | Trên Khu vực tìm kiếm của tất cả màn hình tra cứu:Trường Droplist là Multi select (Cho chọn nhiều) |

  

#### **2.2 Quy chuẩn Component**

|  |  |  |  |  |  |  |  |  |  |  |
| :-- | :-- | :-- | :-- | :-- | :-- | :-- | :-- | :-- | :-- | :-- |
| STT | Loại Component | Quy tắc chung |  |  |  |  |  |  |  |  |
| I | CÁC TRƯỜNG NHẬP LIỆU (Input)Trong các màn hình nhập liệu, các trường bắt buộc phải nhập cần phải đánh dấu (*) bên cạnh để phân biệt. |  |  |  |  |  |  |  |  |  |
| 1 | Textbox | Tự động chặn ký tự khi nhập quá maxlengthChặn nhập ký tự không hợp lệ kèm thông báo inline HT hướng dẫn nhập đúng cho người dùng. VD: Chỉ được nhập text không dấu, không chứa các ký tự: , ! ...Tự động cắt khoảng trắng đầu cuối khi rời khỏi ô nhập.Tìm kiếm theo Textbox là tìm kiếm Like chuỗi ký tự được nhập vào, không phân biệt hoa thường, tìm kiếm theo cả chữ không dấu và có dấu.HT trim đầu cuối khi Submit và trả thông báo kèm thông báo inline HT hướng dẫn nhập đúng cho người dùng |  |  |  |  |  |  |  |  |
|  | Đối với trường Email | Maxlength = 254 kí tựchặn nhập khoảng trắng, Tự động cắt khoảng trắng đầu và cuối khi thoát ô nhậpkhông chứa các ký tự đặc biệt: !, #, $, %, ^, &, *, (, )Cấu trúc địa chỉ email như sau:Chứa ký tự “@”: Phân tách giữa phần tên người dùng và tên miền.Phần tên người dùng (local-part): Có thể chứa chữ cái (a-z, A-Z), chữ số (0-9) và các ký tự đặc biệt như dấu chấm (.), gạch dưới (_), gạch nối (-). Tuy nhiên, không được bắt đầu hoặc kết thúc bằng dấu chấm, và không được có hai dấu chấm liên tiếp.Phần tên miền (domain): Gồm các nhãn được phân tách bằng dấu chấm, mỗi nhãn có thể chứa chữ cái, chữ số và dấu gạch nối (-), nhưng không được bắt đầu hoặc kết thúc bằng dấu gạch nối. |  |  |  |  |  |  |  |  |
| 2 | Input Number | Tự động format “.” phân cách hàng nghìn.Chỉ cho nhập số và kí tự "-", chặn ký tự không hợp lệ (trừ dấu “,” cho thập phân).Validate tức thời giá trị min/max.Căn phải dữ liệu |  |  |  |  |  |  |  |  |
| 3 | Input TextArea | Hỗ trợ tự động thay đổi chiều cao (autoSize) theo nội dung (Auto resize), cần giới hạn maxRows để tránh vỡ layout.Hiển thị bộ đếm ký tự "đã dùng/tối đa" (ví dụ: 150/500) theo thời gian thực.Chặn paste vượt quá maxLength, cắt bớt phần dư và thông báo inline cho người dùng.Trim khoảng trắng đầu/cuối khi blur. Trim khi submit. |  |  |  |  |  |  |  |  |
| 4 | Dropdown | Hỗ trợ tìm kiếm nội dung (filter) khi danh sách có từ 10 items trở lên. Mặc định hiển thị 10 items. Nếu danh sách lớn (>10 items) phải dùng virtual scroll để tối ưu hiệu năng.Gõ và dừng gõ từ 300 miligiay thì mới gọi API để tìm kiếm.Khi dữ liệu đang tải: hiển thị trạng thái loading spinner bên trong dropdown.Khi không có kết quả tìm kiếm: hiển thị thông báo "Không tìm thấy dữ liệu" thay vì dropdown trống.Hỗ trợ chọn bằng con trỏ chuột và bàn phím.Khi submit nếu trường này required mà chưa chọn → Thông báo inline |  |  |  |  |  |  |  |  |
| 5 | DatePicker | Định dạng ngày mặc định theo chuẩn hệ thống: DD/MM/YYYY (Những nghiệp vụ cần hiển thị DD/mm/yyyy hh:mm sẽ được mô tả chi tiết tại nghiệp vụ đó)Nếu tìm kiếm Từ ngày - Đến ngày thì mặc định để trốngKiểm tra tính hợp lệ của ngày khi nhập tay. Hiển thị lỗi ngay khi blur.DateRangePicker: ngày kết thúc phải >= ngày bắt đầu. Tự động cảnh báo nếu vi phạm.Cho phép user chọn ngày từ calendar hoặc nhập ngày vào calendar. Trường hợp nhập dữ liệu không hợp lệ (chữ, ký tự đặc biệt khác "/", ngày không tồn tại (ví dụ ngày 30/12/200)) thì hiển thị lỗi ngay khi blur.Nếu nghiệp vụ giới hạn thời gian tìm kiếm, hệ thống hiển thị thông báo inline: "Thời gian tìm kiếm không được vượt quá N ngày" Lúc nhập dữ liệu: Ko cho nhập ngày nhỏ hơn 1/1/1900Lúc Tìm kiếm: Cho chọn ngày tự do |  |  |  |  |  |  |  |  |
| 6 | Checkbox | Nếu có checkbox “Chọn tất cả” thì phải hỗ trợ 3 trạng thái: unchecked, checked, indeterminate.    Trạng thái indeterminate hiển thị khi có ít nhất 1 mục được chọn nhưng chưa đủ toàn bộ trong list giá trị.Hỗ trợ bàn phím: Space để toggle, Tab để di chuyển giữa các checkbox trong nhóm.Không cho submit nếu trường này required mà chưa chọn.Nếu vượt quá 6 options thì chuyển về combobox |  |  |  |  |  |  |  |  |
| 7 | Radio button | Dùng cho các lựa chọn có ít hơn 6 options.Không cho submit nếu trường này required mà chưa chọn.Khi có > 5 lựa chọn, chuyển sang sử dụng Droplist thay thế. |  |  |  |  |  |  |  |  |
| 8 | Upload (Tải lên file) | Kiểm tra loại file (accept) và kích thước tối đa (maxSize) trước khi upload; hiển thị lỗi cụ thể.Hiển thị thanh tiến độ (progress bar) trong quá trình upload; cho phép hủy upload đang thực hiện.Sau upload thành công: hiển thị tên file, kích thước và icon xóa.Giới hạn số lượng file có thể upload cùng lúc và thông báo rõ khi vượt giới hạn.Có hyperlink: "Tải file mẫu" để hỗ trợ người dùng tải về đúng định dạng file.Không cho phép upload cùng một lúc các file trùng tên (Trùng không phân biệt hoa, thường) |  |  |  |  |  |  |  |  |
| 9 | Switch | Phải có nhãn trạng thái rõ ràng bên cạnh (Bật/Tắt).Hỗ trợ trạng thái loading khi đang xử lý để tránh click nhiều lần. |  |  |  |  |  |  |  |  |
| 10 | Slider (Thanh trượt) | Luôn hiển thị giá trị hiện tại (Tooltip hoặc số trực tiếp) khi kéo thumb của thanh trượt.Đảm bảo giá trị nằm trong khoảng [min, max]; step phải là ước số của (max - min).Hỗ trợ điều khiển bằng bàn phím (mũi tên trái/phải để thay đổi giá trị). |  |  |  |  |  |  |  |  |
| 11 | Combobox | Validate khi blur + submitNếu option dài → hiện tooltipClick vào toàn bộ vùng control đều mở dropdown (không chỉ icon)Khi số lượng option > 10 -> Bắt buộc có search trong dropdownMặc định hiển thị 10 items → Phần list giá trị còn lại sẽ:Load khi người dùng scroll xuốngHoặc load khi người dùng searchNếu Người dùng chọn nhiều giá trị (items) thì hiển thị: item 1 + N items và có icon "x" để bỏ chọn giá trị(VD: D11+5 tệp khác) |  |  |  |  |  |  |  |  |
| II | BUTTONMH Thêm mới gồm: Lưu, Lưu và Gửi duyệt (tuỳ nghiệp vụ), HuỷMH Chỉnh sửa gồm: Lưu, HuỷMH Xem chi tiết gồm: Đóng (Tùy từng nghiệp có chức năng xóa, sửa) |  |  |  |  |  |  |  |  |  |
| 1 | Quy định chung | Chữ trong button là chữ in thường, chữ cái đầu in hoa. Chỉ có duy nhất một Primary Button trong một khu vực hành động; Secondary/Default dành cho hành động phụ. Tự động disable và hiển thị loading spinner khi đang xử lý để tránh submit nhiều lần. Cụm nút hành động chính của Form/Khai báo phải được căn giữa theo chiều ngang của form (Center align). Trong bảng không có đường kẻ dọc, chỉ có đường kẻ ngang (màu nhạt) để ngăn cách các dòng trong bảng |  |  |  |  |  |  |  |  |
| 2 | Button Xuất Excel (Hoặc icon Xuất excel) | Click button/icon "Xuất excel" hiển thị 2 button là: "Xuất trang hiện tại", "Xuất theo bộ lọc"Sau khi xuất thành công tại các màn danh sách thì Format tên file: <tên danh sách>-ddmmyyyyhhmmss,Trong đó:- Tên danh sách: Tên tiếng việt không dấu- ddmmyyyyhhmmss là thời gian tải file về thành công (ví dụ: Danh sách quy tắc xác định mã CIC-25102024170930) |  |  |  |  |  |  |  |  |
| 3 | Ấn Button Tìm kiếm | Sau khi ấn button Tìm kiếm: hiện ra kết quả ở màn danh sách. Sau đó NSD f5 trang hoặc chuyển sang page khác rồi quay lại màn tìm kiếm  → Hệ thống cần giữ được "Tùy chọn bộ lọc" và "cấu hình hiển thị" đã chọn trước đó (không lưu giá trị). |  |  |  |  |  |  |  |  |
| III | HIỂN THỊ DỮ LIỆU |  |  |  |  |  |  |  |  |  |
| 1 | Danh sách kết quả tìm kiếm |  |  |  |  |  |  |  |  |  |
| 1.1 | Quy tắc chung | Tại tất cả màn hình Danh sách kết quả (hoặc màn hình tìm kiếm) phải có icon "mở rộng" để mở full screen của màn danh sáchTại tất cả các cột (ngoại trừ STT, Thao tác) đều cung cấp Filter để phục vụ filter các giá trị trên trang hiển thị.Khi đang tải dữ liệu: hiển thị skeleton loading thay vì bảng trống.Khi không có dữ liệu: hiển thị "Không có dữ liệu" cùng với icon.Virtual scroll bắt buộc khi hiển thị >= 20 dòng trong một trangHành động hàng loạt (batch action): chỉ hiển thị thanh action khi đã chọn ít nhất 1 dòng.Trạng thái mở/đóng (collapse/accordion) của một panel cần được lưu lại trong session để không mất khi tải lại trang.- Accordion mode (chỉ mở 1 panel) dùng khi nội dung có tính loại trừ nhau.- Header của panel phải là element có thể focus và điều khiển bằng Enter/Space.Cột Cố định: STT, Thao tác |  |  |  |  |  |  |  |  |
| 1.2 | Các cột Ngày (Ngày tạo, Ngày cập nhật, Ngày duyệt,...) | Hiển thị định dạng dd/mm/yyyy, khi hover vào sẽ hiển thị dạng đầy đủ dd/mm/yyyy HH:MM:SS |  |  |  |  |  |  |  |  |
| 1.3 | Các cột Người | Hiển thị username, khi hover vào sẽ hiển thị Họ và tên đầy đủ của user đó |  |  |  |  |  |  |  |  |
| 1.4 | Tên cột chỉ ngày, chỉ người (trên màn hình + trong popup Cấu hình hiển thị") | Dùng chung text sau:Người gửi/cập nhật/phê duyệt...;Ngày gửi/cập nhật/báo cáo... |  |  |  |  |  |  |  |  |
| 1.5 | Cột trạng thái | Dùng Tag/Badge. |  |  |  |  |  |  |  |  |
| 1.6 | Căn lề | Header: Căn giữaGiá trị cột Căn trái: TextCăn phải: SốCăn giữa: Ngày tháng, STT, Thao tác |  |  |  |  |  |  |  |  |
| 1.7 | Filter trên từng cột của bảng kết quả tìm kiếm | Chỉ lọc giá trị trong cột trên trang đang hiển thịCho nhập giá trị để lọcCó icon "x" để xóa giá trị đã nhập lọc |  |  |  |  |  |  |  |  |
| 2 | Phân trang | Việc phân trang số lượng bản ghi do BE quyếtMặc định hiển thị 20 dòng/trang.Hiển thị tổng số bản ghi ("Hiển thị 1–20 trong 350 kết quả") bên cạnh hoặc bên dưới phân trang.Cho phép thay đổi số dòng/trang (pageSize) với các tùy chọn: 10, 20, 50, 100,...; ghi nhớ lựa chọn người dùng.Khi kết quả tìm kiếm thay đổi, tự động về trang 1.Cho phép nhập trang muốn chuyển đến là số nguyên dương.Giới hạn pagesize theo cấu hình hệ thống |  |  |  |  |  |  |  |  |
| 3 | Màn hình Xem chi tiết | Hiển thị dạng popup (Nghiệp vụ nào cần hiển thị ở dạng Page sẽ được note rõ trong tài liệu nghiệp vụ đó)Trên tất cả màn hình Xem chi tiết bắt buộc phải có Bảng Lịch sử thay đổi để thống kê lại các lần cập nhật (làm thay đổi) dữ liệu của người dùng/hệ thống. (Các trạng thái trong workflow mà chưa làm thay đổi dữ liệu thì không thể hiện ở bảng này)Bảng Lịch sử thay đổi gồm các trường: STTThời gianNgười cập nhậtHành độngGiá trị cũGiá trị mớiĐịa chỉ IPMô tả   - Thông tin bảng này mặc định thu gọn Collapse Bảng lịch sử thay đổi, Người dùng click vào collapse mới show chi tiết.   - Cột Mô tả: Nếu lượng thông tin vượt quá 2 dòng thì hiển thị .... và nút "Xem tiếp" ở cuối để hiển thị toàn bộ nội dung. Nếu có file đính kèm thì hiển thị trong cột này dưới dạng link. Click link thì tải về.   - Bảng lịch sử hiển thị có scroll, không phân trang, chiều cao tối đa 250px, mặc định sắp xếp thứ tự mới nhất ở trên cùng. | STT | Thời gian | Người cập nhật | Hành động | Giá trị cũ | Giá trị mới | Địa chỉ IP | Mô tả |
| STT | Thời gian | Người cập nhật | Hành động | Giá trị cũ | Giá trị mới | Địa chỉ IP | Mô tả |  |  |  |
| 4 | Các màn hình:Cập nhậtVô hiệu hóaKích hoạtPhê duyệtPhê duyệtXóa | Đều có trường "Lý do", mặc định là không bắt buộc nhập, trường hợp nghiệp vụ bắt buộc nhập Lý do sẽ được mô tả trong giao diện của chức năng cụ thể.- Label: Lý do- Component: Text Area (1000) |  |  |  |  |  |  |  |  |
| 5 | Tên của các button có trong màn Danh sách kết quả tìm kiếm: | Thống nhất tên của các button tính năng như sau:Hiệu lựcHết hiệu lựcGửi duyệtPhê duyệtTừ chốiXem chi tiếtChỉnh sửaXóa |  |  |  |  |  |  |  |  |