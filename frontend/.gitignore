# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependency directories
node_modules/
dist
dist-ssr
*.local

# Next.js build output
.next/
out/
frontend/.next/

# Production build
build/

# Environmental variables
.env
.env.local
.env.production

# AI providers config (chứa key — không commit; xem config/ai-providers.example.json)
config/ai-providers.json

# System prompt "sống" — chỉnh trên server, không commit để git pull không ghi đè.
# Bản mẫu: config/srs-prompt.example.md
config/srs-prompt.md

# Template Word "sống" — chỉnh trên server, không commit. Bản mẫu: srs-template.example.docx
config/srs-template.docx

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
